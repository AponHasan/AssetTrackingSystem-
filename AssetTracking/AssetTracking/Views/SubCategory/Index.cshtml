@model IEnumerable<AssetTracking.Models.Models.SubCategory>

@{
    ViewBag.Title = "Sub-Category";
}

<div class="row">
    <div class="col-md-12">
        <!-- BEGIN TABLE PORTLET-->
        <div class="portlet box green">
            <div class="portlet-title">
                <div class="caption">
                    <i class="fa fa-comments"></i>Sub-Category Index
                </div>
                <div class="tools">
                    <a href="javascript:;" class="collapse"> </a>
                    <a href="#portlet-config" data-toggle="modal" class="config"> </a>
                    <a href="javascript:;" class="reload"> </a>
                    <a href="javascript:;" class="remove"> </a>
                </div>
            </div>
            <div class="portlet-body">
                <p>
                    @Html.ActionLink("Create New", "Create", "SubCategory", null, new { @class = "btn btn-sm green-jungle" })
                </p>
                <div class="table-scrollable">
                    <table id="dataTablefunction" class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Category.CategoryName)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.SubCategoryName)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.SubCategoryCode)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.SubCategoryDescription)
                                </th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Category.CategoryName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.SubCategoryName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.SubCategoryCode)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.SubCategoryDescription)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Edit", "Edit", "SubCategory", new { id = item.SubCategoryID }, new { @class = "label label-sm label-info" }) |
                                        @Html.ActionLink("Details", "Details", "SubCategory", new { id = item.SubCategoryID }, new { @class = "label label-sm label-warning" }) |
                                        @Html.ActionLink("Delete", "Delete", "SubCategory", new { id = item.SubCategoryID }, new { @class = "label label-sm label-danger" })
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <!-- END TABLE PORTLET-->
    </div>
</div>
@section scripts{
    <script src="~/Scripts/DT.js"></script>
}