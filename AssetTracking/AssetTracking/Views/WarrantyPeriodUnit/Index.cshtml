@model IEnumerable<AssetTracking.Models.Models.WarrantyPeriodUnit>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WarrantyPeriodUnitName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WarrantyPeriodUnitName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WarrantyPeriodUnitID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WarrantyPeriodUnitID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WarrantyPeriodUnitID })
        </td>
    </tr>
}

</table>
